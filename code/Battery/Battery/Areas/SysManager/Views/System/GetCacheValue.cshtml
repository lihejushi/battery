@model List<string>
@{
    ViewBag.Title = "缓存列表";
}

<div class="row">
  <div class="col-md-3">&nbsp;</div>
  <div class="col-md-6">
      <div class="panel panel-danger">
        <div class="panel-heading">
            <h3 class="panel-title">缓存列表</h3>
        </div>
        <div class="panel-body">
            <ul class="list-group">
                @foreach (var item in Model)
                {
                    <li class="list-group-item"><a href="?key=@Html.Raw(item)" target="_blank">@Html.Raw(item)</a></li>
                }
            </ul>
        </div>
    </div>

  </div>
  <div class="col-md-3">&nbsp;</div>
</div>